using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum Genre { Romance, Comedy, Action, Documentary, Reality };

public enum Timeslot { Morning, Daytime, PrimeTime, Night }

public enum Demographic { Toddlers, ArmchairAthletes, ClosetRomantics, AdrenalineJunkies }